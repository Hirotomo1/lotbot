export interface IinitialState {
  margin: number;
  pips: number;
  percentage: number
  answer: number
}

const initialState : IinitialState = {
    margin: 0,
    pips: 0,
    percentage: 0,
    answer: 0
}

export default initialState